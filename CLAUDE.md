# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 项目概述

佛经句读（标点）项目。将 InDesign 排版的佛经文本导出为纯文本，清除原有标点后，按规则重新添加句读符号（中文句号「。」），输出句读结果。

## 核心文件

- `WD句读工作流程.md` — 8 步处理流程（步骤0-7），含铁律、8轮规则应用顺序、分块策略、重建法
- `网页版句读规则.md` — 35 条句读细则 + 固定短语处理表
- `{编号}从ID中导出文字.txt` — 从 InDesign 导出的原始文本（输入）
- `{编号}从ID中导出文字_WD句读结果.md` — 句读后的结果文件（输出）
- `{编号}导出.idml` / `定稿未改动/{编号}定稿未改动.indd` — InDesign 源文件

## 处理流程（8 步）

0. **清除原文标点** — 移除所有中英文标点、空格，保留段落换行；记录清除前的原文留底
1. **预处理/文件分析** — 统计字数、识别结构（经题、卷次、品名、段落标志词）
2. **分块策略** — ≤3000字不分块；3000-10000字分2-4块；>10000字按卷/品切分（每块≤2500字），带300字上下文窗口
3. **逐块处理（8轮规则）** — 详见工作流程文档的轮次表
4. **跨块接缝检查** — 检查块边界的句子是否被误断或漏断
5. **全文终检** — 字数校验（原文字数 = 输出字数 - 新增句号数）+ 逐字对照
6. **输出规范** — 命名为 `{原文件名}_WD句读结果.md`，头部注明统计信息，使用「。」作为句读符号
7. **质量标准** — 原文零改动、字数校验通过、逐字对照通过、全部规则覆盖

## 三条铁律（不可违反）

1. **原文零改动** — 句读符号仅可插入文字之间，不得增、删、改原文任何文字
2. **交付前反思** — 逐字对照原文检查，确认无任何文字改动后方可提交
3. **不得参考原标点** — 句读时必须使用已清除标点的纯文本，不得参考原文中的原有标点

## 关键技巧：重建法

当手工输出句读结果出现异体字替换（如"虚"→"虛"）等问题时，使用 Python 脚本将所有非句号字符替换回原文对应位置的字符，确保 100% 字符保真。具体算法见 `WD句读工作流程.md` 第三部分。

## 句读规则要点

- 规则按 8 轮顺序应用，不可跳跃
- 不凭感觉，逐条匹配；不确定处标记 `[?]`
- 以不破坏佛经原有教义、义理为前提
- 固定短语（如"善哉善哉→善哉。善哉。"）在第 1 轮优先处理

## 文件命名规范

- 输入：`{编号}从ID中导出文字.txt`
- 输出：`{编号}从ID中导出文字_WD句读结果.md`
- 规则：`网页版句读规则.md`
- 流程：`WD句读工作流程.md`
