# v1.5.2 方案：标点重分配（CSR 复用）与括号保留

- 日期：2026-08-07
- 状态：**已确认，待实施**（方案文档，实施前需再次确认）
- 版本基线：v1.5.1（即当前 `code/inject.py`，2026-08-06 17:42）
- 涉及文件：`code/inject.py`、回归脚本（新增 `code/reg_v152.py`）、备份

---

## 一、背景与目标

### 1.1 两个问题

**问题 1（括号保留）**：IDML 原文含「（）」等成对符号（括号/书名号/引号），txt 句读结果对应位置没有这些符号。回注后 IDML 原位置的括号被清除，需要保留。

**问题 2（混合标点导致换行 + 文件膨胀）**：35 号文件用混合标点 txt（815 句号 + 702 逗号 + 190 顿号）回注后，IDML 中「時、離垢施」出现换行/换段；且输出 Story 体积膨胀 2.73 倍（1.05MB → 2.87MB）。只有句号 txt 在该位置无标点，视觉正常。

### 1.2 目标

- 问题 2：消除逐字 CSR 拆分导致的体积膨胀与排版异常，恢复与原文一致的 CSR 结构
- 问题 1：回注时保留 IDML 原位置的成对符号
- 回归：在方案 C 必然改变 CSR 结构的前提下，保持验证强度不降级（分层验证）

---

## 二、问题 2 诊断结论（根因已确认）

### 2.1 现象对比

| 版本 | 该位置文本 | CSR 结构 |
|---|---|---|
| 原始 IDML | 時。離垢施女。則為梵志而說頌曰。 | `時`一个CSR → `。`句号CSR → `離垢施女`一个CSR → `。` → `則為梵志而說頌曰`一个CSR |
| 只有句号版 | 時離垢施女則為梵志…（无标点） | 该位置无注入标点，CSR 保持完整 → 视觉正常 |
| 混合版 | 時、離垢施女、則為梵志… | 「時」「、」「離」「垢」「施」「女」…**逐字拆成独立 CSR** → 换行 |

### 2.2 根因链条

1. 对齐逻辑把 txt 标点分配到「它跟随的文字 CSR」上（`after_csr`），使该 CSR 判定为内部有标点（`has_punct_inside=True`）；
2. `_rebuild_paragraph_xml` 一旦判定有内部标点，走拆分路径——把该 CSR 的**每个文字字符**拆成独立 `<CharacterStyleRange>`（每个带约 300 字节完整模板）；
3. 混合版在「時」后、「離垢施女」后各有一个顿号 → 两个 CSR 被逐字拆散；
4. 大量单字 CSR + 顿号（InDesign 禁行首字符）→ 排版引擎断行异常。

### 2.3 数据佐证（35 号 Story_u621.xml）

- 原始 1.05MB → 混合版输出 2.87MB（膨胀 2.73 倍）
- CSR 数 3651 → 9396；单字 Content 1922 → 8640
- 内容**没有丢失**（净文本 11227 = 原文 11040 + 注入标点），验证能通过，但排版被破坏
- 只有句号版同样存在拆分（8632 CSRs），只是该位置无标点未触发视觉问题

### 2.4 关键发现：v1.5.0 的「旧句号 CSR 复用分支」是死代码

原始 IDML 中文字 CSR 后几乎都紧跟一个句号 CSR（`CharacterStyle/句号`，带 FillColor 红色样式）。代码本有「旧句号 CSR 复用」分支（标点写进这些 CSR），但**标点在对齐时被分配给了前邻文字 CSR（after_csr），旧句号 CSR 永远收不到标点** → 该分支从不触发 → 旧句号 CSR 被清空、文字 CSR 被迫拆分。

---

## 三、方案 C：标点重分配（问题 2 修复）

### 3.1 核心机制

原始 IDML 中每个文字 CSR 后几乎都紧跟一个句号 CSR。txt 标点与旧句号位置重合时（句读位置相同、符号不同），把标点**转移进旧句号 CSR**——文字 CSR 零改动，旧句号 CSR 仅把 Content 从「。」换成实际标点。

### 3.2 改动点 1：末尾标点重分配预处理

**位置**：`_rebuild_paragraph_xml` 内，csr_segments 构建（约 line 1353）之后、替换生成（约 line 1392）之前。

**逻辑**：

```
对每个文字 CSR ci（有 segments 且非 punct）：
  定位 CSR 内最后文字字符位置 (last_slot, last_pos)
  若 segments 中有标点 (slot == last_slot 且 pos >= last_pos)：
    且 ci+1 是旧句号 CSR（is_punct=True）且其 segments 为空：
      把该标点从 csr_segments[ci] 转移到 csr_segments[ci+1]
```

**实现要点**：
- 转移后文字 CSR 无内部标点 → 走现有「不拆分合并」路径 → Content 原样重建，零改动
- 多 Content CSR 同样适用（末尾标点 = 最后槽位末字符后）
- 转移顺序：多个标点保持原顺序

### 3.3 改动点 2：旧句号 CSR 复用分支改造（约 line 1474-1507）

**现状问题**：
- 用全局第一个「。」CSR 做模板，丢失各 CSR 自身样式
- 不处理 Group/Self（分割副本 ID 冲突）
- 多标点时会复制 Br

**改造**：
- 改用 CSR **自身模板**（`orig_csr`），保留各自 PointSize/FillColor/Br
- 副本时剥离 Group + Self
- 单标点直接替换；多标点去 Br 拼接后末尾补一个 Br

**新增辅助函数** `_punct_csr_from_own_template(template, char)`：
- 优先替换含「。」的 Content
- 无「。」则替换第一个非空 Content
- 其余标签（含 Br）原样保留

### 3.4 改动点 3：拆分路径合并连续文字段（兜底，约 line 1613-1644）

无法重分配的标点（AI 在原文无句号处新加的标点、CSR 中间标点）仍走拆分路径，但**连续文字 segment 合并为一个 CSR**：

```
现在：等|輩|求|諸|利|義|必|不|如|意|。  → 11 个 CSR
修复：等輩求諸利義必不如意|。         → 2 个 CSR
```

**实现要点**：
- 第一个 part 保留原 `csr_prefix`（含 Self/Br/Group），其余用 `clean_prefix`（剥离）
- 最后一个 part 的 suffix 用 `csr_suffix`（保留原 CSR 尾部 Br/空白）——与现有逻辑一致
- 标点 part 继承前邻文字块的前缀（强制思源宋体）

### 3.5 边界情况

| 场景 | 处理路径 |
|---|---|
| 标点在 CSR 末尾 + 后跟旧句号 CSR | 重分配 → 复用 ✓（主路径） |
| 标点在 CSR 末尾 + 后无旧句号 CSR（PSR 末尾/后是文字 CSR） | 不重分配 → 改动点 3 兜底 |
| 标点在 CSR 中间（AI 在长句内加逗号） | 不重分配 → 改动点 3 兜底 |
| 旧句号 CSR 含 Br（偈颂分行） | 复用且保留 Br |
| 分割副本中的旧句号 CSR | 复用 + 剥离 Group/Self |
| 多 Content CSR（偈颂） | 末尾标点重分配；内部标点槽内插入（现有逻辑不动） |
| 标点被抑制（仿宋/楷体、U+3000） | 不生成记录，无影响 |

### 3.6 预期效果（35 号「時、離垢施女、則為梵志而說頌曰。」）

```
输出结构：時 CSR + 、(旧句号CSR复用) + 離垢施女 CSR + 、(复用) + 則為梵志而說頌曰 CSR + 。(复用)
CSR 数量：≈ 原始 3651（当前 9396）
输出体积：≈ 原始 1.05MB（当前 2.87MB）
视觉：与原文排版一致，无逐字拆分，无换行异常
标点样式：沿用旧句号 CSR 自身模板（红色样式问题按用户意见不做额外处理）
```

---

## 四、分层验证方案（回归体系）

### 4.1 基线

- **基线 = 当前 `code/inject.py`（v1.5.1）**，实施第一步备份为 `backup/inject.py.v1.5.1-csr-reuse-baseline-20260807.bak`
- 对比对象：
  - L1/L2：v1.5.1 输出 vs v1.5.2 输出（验证方案 C 相对现状只改预期结构）
  - L3：v1.5.2 输出 vs **原始 IDML**（验证文字 CSR 与原文零偏差）

### 4.2 L1 净文本对比（铁律层）

| 项 | 内容 |
|---|---|
| 函数 | 复用现有 `story_text_contents()`（Content 拼接、去 ACE 指令、解实体） |
| 对比 | 每 Story：v1.5.1 vs v1.5.2 净文本**完全一致** |
| 增强 | 差异时分解输出「文字序列 diff」「标点序列 diff」两条诊断 |
| 判定 | 100% 一致，任何差异即失败 |

覆盖：原文文字零改动铁律、标点注入正确性、U+3000 分字间隔。

### 4.3 L2 结构指标对比（靶向层）

新增 `story_metrics(path)`，每 Story 统计 6 项指标：

| 指标 | 定义 | 对比规则 |
|---|---|---|
| `br` | `<Br` 计数 | **必须相等**（硬性）——方案 C 不该动 Br |
| `psr` | `<ParagraphStyleRange` 计数 | **必须相等**（硬性）——段落结构不动 |
| `csr` | `<CharacterStyleRange` 计数 | 新 ≤ 旧（只减不增） |
| `single1` | 单字符 Content 的 CSR 数 | 新 < 旧 × 0.5（逐字拆分显著消除） |
| `oldpunct` | 含 `CharacterStyle/句号` 样式的 CSR 数 | 与原始 IDML 一致 ±10% |
| `emptycsr` | Content 全空的 CSR 数 | 显著下降（旧句号不再被清空成空壳） |

**补充指标**：`preserved_multichar(orig, new)` —— 原始 IDML 中多字文字 CSR 在 v1.5.2 中**保持为完整单 CSR 的比例**，≥ 95%。这是方案 C 主路径生效的直接证据。

### 4.4 L3 样式与复用抽查

| 检查项 | 方法 | 判定 |
|---|---|---|
| 文字 CSR 样式 | 抽样 20 个文字 CSR，对比原始 IDML：AppliedFont / PointSize / FillColor 三元组 | 完全一致 |
| 复用句号 CSR | v1.5.2 中所有含 `CharacterStyle/句号` 的 CSR：Content ∈ 白名单标点（非「。」非空）、FillColor/PointSize 与原始对应一致、含 Br 的保留 Br | 全部满足 |
| 兜底新标点 CSR | 无法重分配的新增标点 CSR：样式 = 思源宋体模板 | 抽查通过 |

### 4.5 验证矩阵

| 用例 | txt 类型 | L1 | L2 重点 | L3 |
|---|---|---|---|---|
| 461 | 纯句号 | 净文本 = 基线 | br/psr 相等、csr 下降 | 样式抽查 |
| 275 | 纯句号 | 净文本 = 基线 | br/psr 相等、分割段空壳不反弹 | 样式抽查 |
| 3093 | 纯句号（偈颂） | 净文本 = 基线 | **br 必须相等**（偈颂分行关键） | 样式抽查 |
| 35 | 混合标点 | 净文本 = **txt** | csr ≈ 原始、体积回落、「時、離垢施」无逐字拆分 | 复用抽查 |

### 4.6 回归脚本结构（`code/reg_v152.py`）

```
OLD = backup/...v1.5.1...baseline-20260807.bak
NEW = code/inject.py
CASES = [(461...), (275...), (3093...)]
SPECIAL = [(35, 混合标点txt...)]

for case in CASES:
    out_old = run(OLD, case); out_new = run(NEW, case)
    L1: story_text_contents 逐 Story 相等 → 失败输出双序列 diff
    L2: br/psr 相等；csr 不增；single1 降 ≥50%
    L3: preserved_multichar ≥ 0.95；样式抽查 0 差异
for case in SPECIAL:
    out_new = run(NEW, case)
    L1: 净文本 vs txt 相等
    L2: csr ≈ 原始（≤原始×1.5）；「時、離垢施」XML 片段无逐字 CSR
    L3: 句号 CSR 复用抽查（Content 为实际标点）
```

### 4.7 失败诊断路径

| 失败现象 | 可能原因 | 定位方法 |
|---|---|---|
| L1 净文本差异 | 重分配错位 / 对齐偏移 | diff Story + 字符上下文 + 文字/标点双序列 |
| L2 br 差异 | 复用分支 Br 处理 bug | 对比 br 位置序列 |
| L2 csr 不降 / single1 不降 | 重分配未触发 / 拆分仍逐字 | 打印重分配统计；检查改动点 3 |
| L3 preserved < 95% | 重分配条件漏掉场景 | 列出未保持完整的多字 CSR |
| L3 样式差异 | 复用模板错误 | 对比样式签名三元组 |

---

## 五、问题 1：括号保留

### 5.1 需求

IDML 原文含成对符号（括号/书名号/引号等），txt 对应位置没有 → 回注时保留 IDML 原位置的符号。

### 5.2 成对符号清单

**项目现有 IDML 实测（86 个文件全量扫描）**：

| 符号 | 出现次数 | 实际用例 |
|---|---|---|
| 全角圆括号 `（）` | 54 | 20 号藏本校勘注：「（甯）（磧）（清）（大）…」 |
| 龟甲括号 `〔〕` | 16 | 3093 偈颂：「慈〔云〕懴主依華嚴經所立」 |
| 其他全部 | 0 | 书名号/引号/方括号等在本项目现有文件中均未出现 |

> 注：全量扫描中的 964 个"半角尖括号"经核实是 ACE 字形加工指令 `<?ACE 18?>`（Content 内嵌指令，FIX-6 已保留），并非真实括号。

**完整成对符号清单（含未来可能遇到的）**：

括号类：
1. 圆括号：全角 `（）`、半角 `()`、小字号变体 `﹙﹚`、竖排 `﹝﹞`
2. 方括号：全角 `［］`、半角 `[]`、实心 `【】`、空心 `〖〗`
3. 花括号：全角 `｛｝`、半角 `{}`
4. 尖括号：全角 `＜＞`、半角 `<>`（**注意**：半角 `< >` 在 XML 内须转义为 `&lt; &gt;`，且与 ACE 指令 `<?ACE N?>` 同形，需特殊甄别）
5. 龟甲括号：`〔〕`、空心 `〘〙`
6. 直角括号：`〚〛`

书名号类：7. 双书名号 `《》`、单书名号 `〈〉`

引号类：8. 直角引号 `「」`、双直角引号 `『』`；9. 弯引号 `“”`、单弯引号 `‘’`、竖排 `〝〞`；10. 法式角引号 `«»`、`‹›`

### 5.3 实现方案（方案 A1：成对符号白名单保留）

1. 代码定义**成对符号白名单常量** `_KEEP_BRACKETS`（默认启用：`（）()〔〕《》〈〉「」『』【】［］` 等常见组）
2. 解析 IDML 时，白名单内的符号字符标记 `is_bracket=True`
3. 对齐时跳过括号记录（不消费 txt 字符，类似 U+3000/ACE 处理）
4. 重建时括号所在 CSR 的 Content **原样保留**
5. txt 中若出现括号则仍拒绝（防原文冒充句读结果）——v1.5.0 白名单校验不变

### 5.4 验证调整（扩展预留）

问题 1 实施后有两处验证语义变化：
1. **净文本对比（L1）**：新版净文本 = 旧版 + 括号字符（括号从 IDML 保留、txt 没有）→ 改为「允许括号增量的差异」
2. **输出自检**：验证输出 vs txt 时须排除括号（与 U+3000/ACE 同类特殊字符处理）
3. `story_metrics` 预留 `bracket` 指标：保留括号数 = 原始数（零丢失）

> 实施顺序：**方案 C（问题 2）验证通过后，再实施括号保留（问题 1）**。

---

## 六、实施步骤与验收标准

### 6.1 实施步骤

1. **备份**：`cp code/inject.py backup/inject.py.v1.5.1-csr-reuse-baseline-20260807.bak`
2. **改动点 1**：新增末尾标点重分配预处理
3. **改动点 2**：新增 `_punct_csr_from_own_template` + 改造旧句号 CSR 复用分支
4. **改动点 3**：拆分路径合并连续文字段
5. **回归脚本**：编写 `code/reg_v152.py`（三层验证：L1 净文本 / L2 结构指标 / L3 样式抽查）
6. **回归执行**：461 / 275 / 3093（基线对比）
7. **35 号专项**：混合标点 txt 重注 + 验证（净文本 vs txt、CSR 数/体积回落、「時、離垢施」结构检查）
8. **版本标记**：更新记忆与版本号 v1.5.2

### 6.2 验收标准

- [ ] 461/275/3093：L1 净文本与基线一致、L2 br/psr 相等且 single1 降 ≥50%、L3 preserved ≥95% 且样式抽查 0 差异
- [ ] 35 号：净文本与 txt 一致、CSR 数 ≤ 原始×1.5、输出体积回落、「時、離垢施」无逐字拆分
- [ ] 无新增 Br/PSR 边界

### 6.3 后续（问题 1，另行确认）

- [ ] 成对符号白名单常量定义
- [ ] 解析/对齐/重建三处修改
- [ ] 验证逻辑调整（净文本括号豁免、bracket 指标）

---

## 七、风险与注意事项

1. **回归脚本语义变化**：字节级对比退出历史舞台，由「净文本 + 结构指标 + 样式抽查」替代。此变更只针对方案 C 涉及的重建逻辑，其余功能（提取/对齐/验证）不受影响
2. **punct_template 全局模板**：改造后复用分支不再使用全局模板，`punct_template` 收集逻辑保留（最小改动），后续可清理
3. **多标点连续**（如「。、」）罕见但需防御：复用分支已处理（去 Br 拼接 + 末尾补 Br）
4. **497 性能**：方案 C 减少拆分 CSR 生成，性能只升不降；497 新句读结果可后续重跑验证
5. **IDML 打开检查**：35 号重注后建议在 InDesign 中人工确认「時、離垢施」位置与偈颂分行
