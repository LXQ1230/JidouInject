# IDML 句读回注工具 — 免 Python 环境使用说明

> 本说明面向目标电脑（无 Python 环境）。程序为**单文件绿色版**，
> 复制 `IDML句读回注工具.exe` 到任意文件夹即可使用，无需安装任何软件。

## 一、使用方式（v1.5.5 起支持三种）

### 方式 1：拖到 exe 图标上（推荐，与旧版 .bat 习惯一致）

把文件**直接拖到 `IDML句读回注工具.exe` 图标上**松手，自动弹出黑色命令行窗口处理，完成后按回车键关闭。支持：

| 拖入内容 | 行为 |
|---|---|
| 2 个文件（IDML + 句读结果，**顺序不限**）| 单文件回注，输出 `{IDML名}_WD注入.idml`（与 IDML 同目录）|
| 只拖 1 个 IDML 文件 | 自动在同目录查找对应句读结果；找不到时弹出文件选择框手动指定 |
| 多个文件（多对）| 自动按经号配对，逐对批量处理 |
| 整个文件夹 | 自动配对文件夹内所有「同经号」文件对批量处理，输出到文件夹下 `output\` 子目录 |

> 之前「必须两个文件同时拖入、且 IDML 在前」的限制已取消——现在任意顺序、任意数量、甚至文件夹都可以。
> 输出文件已存在时自动加 `_v2`/`_v3` 后缀，绝不覆盖。

### 方式 2：拖进图形界面窗口内（v1.5.5 新增）

1. 双击 `IDML句读回注工具.exe` 打开图形窗口。
2. 把文件或文件夹**直接拖进窗口内**松手，自动识别并载入：
   - 单对文件 → 自动填入「IDML 文件 / 句读结果」输入框，核对后点「开始回注」；
   - 只拖 IDML → 载入 IDML 并提示补拖句读结果（可分两次拖：先 IDML 后结果）；
   - 文件夹 / 多对 → 弹出批量确认窗口（可改输出目录），确认后开始。
3. 处理中（正在回注）时拖入会被忽略，避免干扰。

### 方式 3：图形界面（双击运行，常规操作）

1. 双击 `IDML句读回注工具.exe`，打开图形窗口。
2. **单文件回注**：分别点「浏览…」选择 IDML 与句读结果，
   输出文件留空则自动生成（与 IDML 同目录）；点「开始回注」。
3. **批量处理**：把多个「同经号」的文件对放进同一个文件夹
   （文件名以数字经号开头，如 `497导出.idml` + `497句读结果.md`），
   点「批量处理…」选择该文件夹，程序自动配对逐个处理。
   输出生成在所选文件夹的 `output\` 子目录下。
4. 处理日志实时显示在窗口下方，出错会弹窗提示。

## 二、文件命名约定（批量模式必需）

| 文件 | 命名要求 | 示例 |
|---|---|---|
| IDML | 以数字经号开头，`.idml` 结尾 | `497导出.idml` |
| 句读结果 | 同经号开头，`.md` / `.txt` 结尾 | `497句读结果.md` |

- 经号相同的 IDML 与句读结果自动配成一对。
- 自动排除：`*_WD注入*.idml`（上次的输出）与 `*_test*`（测试文件），可放心重复使用同一文件夹。
- 同名经号多个文件时：IDML 优先选含「导出」的；结果按文件名排序取第一个。

## 三、安全与覆盖规则

- **绝不覆盖**已有文件：输出文件已存在时自动加 `_v2`、`_v3` 后缀重命名。
- 单文件模式下若输出已存在，会弹窗让你选择：覆盖 / 自动重命名 / 取消。
- 输出文件不能与 IDML 源文件同一路径（防止覆盖源文件）。

## 四、常见问题

| 问题 | 处理 |
|---|---|
| 拖拽后黑窗一闪而过 | 不会发生——程序会在处理结束后等待按回车 |
| 提示「文件不是有效的 IDML」 | 该文件不是标准 IDML（可能被改名或损坏） |
| 只拖 1 个 IDML 弹出选择框 | 程序没找到对应句读结果（文件名经号不一致），手动指定即可 |
| 提示「缺少 IDML 文件」 | 只拖入了句读结果，需要同时拖入对应的 IDML 文件 |
| 杀毒软件误报 | 首次生成的 exe 可能被误报，添加信任即可（Nuitka 打包程序常见） |
| 处理大经书较慢 | 属正常现象，进度条会实时显示百分比 |

## 五、版本

- 程序版本：v1.5.5（新增/增强拖拽：① 图标拖拽支持单文件自动找结果、批量多对、文件夹、顺序自适应；② 新增窗口内拖拽；③ 拖拽处理输出冲突自动 _v2 重命名。此前 v1.5.4 改进 GUI 批量确认与响应速度，v1.5.3 支持括号「（）（）〔〕」等成对符号与几何装饰符号「■□△▲○●◇◆」等保留，v1.5.2 优化标点重分配消除体积膨胀）
- 打包方式：Nuitka 4.1.3（Python 3.12.10），纯标准库 + tkinter，无任何第三方运行时依赖
- 命令行兼容：`exe --idml <文件> --result <文件>`（老用法仍可用）
