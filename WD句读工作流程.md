# WD句读工作流程

> 本文档是佛经句读的完整工作流程手册。使用时需配合《句读规则》文件（35条细则 + 固定短语表），本文档仅描述流程框架和操作规范，不包含具体规则内容。

---

## 一、规则体系框架

### 铁律（优先级最高，任何情况下不可违反）

1. **原文零改动**：句读符号仅可插入文字之间，不得对原文文字进行任何增、删、改操作。
2. **交付前反思**：每次句读完成后，必须逐字对照原文进行反思检查，确认无任何文字改动后方可提交。
3. **不得参考原标点**：句读时必须使用已清除标点的纯文本，不得查看或参考原文中的原有标点符号。句读判断应完全基于规则，而非原文的原有断句。

### 句读规则（由用户另行提供）

由用户提供《句读规则》文件，包含约35条细则和若干固定短语处理规范。规则应用按以下8轮顺序执行：

| 轮次 | 范畴 | 说明 |
|------|------|------|
| 第1轮 | 锁定不可拆分项 | 先锁定固定词组和完全合并项，避免后续规则误拆 |
| 第2轮 | 固定拆分模式 | 法数四字拆分、佛十号拆分等 |
| 第3轮 | 题记与时间状语 | 经题题记、一时/尒時等时间词 |
| 第4轮 | 并列与排比 | 三组以上并列、若字句、排比句、数字、人名地名 |
| 第5轮 | 对话与称呼 | 唯/如是/善哉后的称呼、xxx说/xxx告等 |
| 第6轮 | 合并规则 | 谓/所谓、颜色、四字结构合并 |
| 第7轮 | 语法规则 | 主语谓语、状语动词、者字、疑问词、主谓结构 |
| 第8轮 | 特殊场景 | 律部论部转述、仪式感（行住坐卧） |

> 规则应用原则：不凭感觉，逐条匹配；不确定处标记 `[?]`；以不破坏佛经原有教义、义理为前提。

### 固定短语（由用户另行提供）

包含若干组固定拆分/合并的短语结构（如"善哉善哉→善哉。善哉。"等），在第1轮规则应用中优先处理。

---

## 二、处理流程（8步）

### 步骤0：清除原文标点（前置必做）

**操作**：移除原文中所有已有标点符号。

**清除范围**：
- 中文标点：。，、；：？！""''「」『』（）【】《》…—～
- 英文标点：.,;:?!""''()[]{}
- 空格、制表符等空白字符（保留段落间的单个换行作为自然分界）

**清除后验证**：
- 确认所有汉字、数字、外文字母连续排列，中间无任何标点符号
- 仅保留段落换行作为结构信息
- 记录清除前的原文（留底备份），清除后的纯文本作为句读输入

> 注意：此步骤仅清除标点符号，汉字文字本身不做任何改动。清除后的纯文本是句读工作的唯一输入，**后续所有步骤不得再参考原始带标点的文本**。

---

### 步骤1：预处理 —— 文件分析

1. **统计规模**：总字数、总行数
2. **识别结构**：扫描全文，标记所有自然断点——
   - 经题、卷次、品名（如"xxx品第x"）
   - 大段空行/换行
   - 明显段落标志词（尒時、復次、佛告、爾時、一時等出现在行首）
3. **判定分块方案**：
   - ≤ 3000 字 → 单块处理，无分块
   - 3000-10000 字 → 按自然断点分 2-4 块
   - > 10000 字 → 按卷/品切分，每块 ≤ 2500 字
   - **任何时候不在句子中间切分**

---

### 步骤2：分块策略

**找自然断点（优先级从高到低）**：
1. 品名/章节标题 —— 最佳切分点
2. 大段空行 —— 次优
3. "尒時/復次/佛告/爾時/一時/於是" 出现在行首 —— 可用
4. 以上皆无时，找已有句读的完整句末切分

**上下文窗口**：
- 每块实际处理时，带上上一块末尾 300 字作为上下文前缀
- 上下文中的句读标记保留，但只作参考，不重复输出

**块管理**：
- 每块赋予编号 `[块N/M]`，记录起止位置
- 单独保存每块的中间结果

---

### 步骤3：逐块处理 —— 规则应用

每块按8轮顺序逐条应用全部规则，不可跳跃。详见上文"一、规则体系框架"中的轮次表。

**每块处理要求**：
- 逐条匹配，不凭感觉
- 不确定性处标记 `[?]`
- 每块完成后自检：原文 N 字 → 输出应为 N 字 + 新增句读符号数

---

### 步骤4：跨块接缝检查

全部块处理完毕后：

1. 拼接所有块的结果
2. 逐块接缝处检查：上一块最后 2 句 + 下一块前 2 句是否被误断或漏断
3. 跨块排比结构是否被破坏（如復次开头的排比段跨越块边界）

---

### 步骤5：全文终检

**字数校验（强制）**：
```
原文总字数 = 输出总字数 - 新增句读符号数
```
两者必须相等，不等则逐块排查。

**逐字对照（强制，铁律第2条）**：
- 从原文第1字到最后一字，与输出逐一比对（去掉输出中的句号后比对）
- 确认：无增字、无删字、无改字、无错位
- 发现任何差异立即修正

**规则覆盖检查**：
- 列出全部规则，逐条确认是否已应用
- 列出固定短语表，逐条确认是否处理

---

### 步骤6：输出规范

1. 输出文件命名为 `{原文件名}_WD句读结果.md`
2. 文件头部注明：
   - 原文路径
   - 原文总字数（清除标点后）
   - 原文总字数（含标点）
   - 清除标点数
   - 分块数
   - 处理时间
3. 如有不确定性标记 `[?]`，在文件末尾汇总列出，供人工复核
4. 句读符号统一使用中文句号「。」
5. 保留原文的段落换行结构

---

### 步骤7：质量标准

- ✅ 原文零改动（铁律1）
- ✅ 字数校验通过
- ✅ 逐字对照通过（铁律2）
- ✅ 全部规则覆盖
- ✅ 固定短语表无遗漏
- ✅ 跨块接缝无损伤
- ✅ 不确定性标记已汇总
- ❌ 绝不使用"差不多""大概看看"的方式处理

---

## 三、关键技巧：重建法（保字存句读）

### 问题背景

在手工输出句读结果时，极易出现以下问题：
- 异体字替换：如将原文的"虚"写成"虛"、"乗"写成"乘"、"无"写成"無"等。这些字符在语义上等价，但违反了铁律1。
- 多余字符插入或遗漏

手工逐一修复效率低且容易遗漏。

### 解决方案：重建法

**核心思路**：句读的本质是"在原文的字符间插入句号"。因此可以先用任何方式产生句读结果，然后用脚本将输出中的非句号字符替换回原文对应位置的字符，确保100%保真。

**操作步骤**：

1. 正常输出句读结果（允许少量字符差异）
2. 用脚本逐字对比原文 vs 输出（去掉句号后），统计差异数
3. 如差异 > 0，执行重建：

```python
# 伪代码：重建逻辑
原文 = 清除标点后的纯文本（无句号）
输出 = 句读结果（含句号）

结果 = []
原文指针 = 0

for 字符 in 输出:
    if 字符 == '。':
        结果.append('。')          # 保留句号
    else:
        结果.append(原文[原文指针])  # 用原文字符替换
        原文指针 += 1

# 验证
assert 结果.去掉句号() == 原文      # 必须完全一致
```

4. 用原文的段落结构重建换行
5. 再次校验，确保差异归零

此方法可一次性消除所有字符替换问题，是保证铁律1的最终防线。

---

## 附录：使用说明

1. 将本文件 + 《句读规则》文件 + 待处理的佛经文本一起提供给AI
2. AI 应先通读本文件理解流程，再读取《句读规则》学习具体规则
3. 按8步流程执行，第3步中按8轮顺序应用规则
4. 输出时优先使用重建法确保字符保真
5. 最终提交前必须通过步骤5的全部校验
