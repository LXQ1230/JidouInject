# JidouInject 完整修复方案

- **制定日期**: 2026-08-04
- **依据**: 四轮深度审查报告（audit-report-2026-08-04.md，已剔除字体抑制误报项）
- **问题总量**: P0 ×1 / P1 ×8 / P2 ×12 / P3 ×11
- **修复原则**: ① 先堵静默数据错误路径，再补健壮性；② 每次改动后跑全量回归（5 本经书）；③ 修复与备份同步（先 commit 再改）。

---

## 第 0 步：前置保护（任何改动之前，10 分钟）

| # | 动作 | 说明 |
|---|------|------|
| 0.1 | `git add -A && git commit -m "chore: v2.0 槽内插句号+流式写回+CharRecord 成果提交保护"` | 当前 `code/inject.py` 相对 HEAD(33fa16a) 的全部 v2.0 改动**未提交**，先落库 |
| 0.2 | 重命名 `backup/v2.0-2026.08.04/` → `backup/v1.4-pre-slot-2026.07.31/` | 该备份实为改造前旧版，命名误导；改造后快照以 git 提交为准 |
| 0.3 | 决定 `35 经书 515 句号抑制` 为设计意图（已确认），在 progress.md 记录抑制基线：275:10 / 461:0 / 0080:2 / 35:515 / 3093:0 | 防后续误判为回归 |

---

## 第一批：P0 + P1（立即执行，预计 2-3 小时）

### FIX-1【P0】错误配对静默注入原文标点 —— 三层修复

**根因**：`extract_from_result()` 不过滤旧标点；`result_compare_chars` 只排除 `。`；佛经导出 txt 标点以 `。` 为主 → 含原标点的文件冒充句读结果时验证自洽通过。

**修复 A（batch 层，堵住入口）** — `code/batch_inject.py` `find_pairs()`：
```python
# 旧：
if (f.endswith(".md") or f.endswith(".txt")) and "_WD注入" not in f:
# 新：强校验句读结果身份，排除导出原文
if (f.endswith(".md") or f.endswith(".txt")) \
   and "句读结果" in f and "_WD注入" not in f:
# 同时 find_pairs 排除 *_old_test* / *_test* / *_导出.txt 模式
```

**修复 B（工具层，兜底单文件模式）** — `code/inject.py` `extract_from_result()`：
```python
# 提取后校验：结果文件不应含「。以外」的旧标点（AI 句读结果只应有文字+句号）
for ch in chars:
    if _is_old_punct(ch) and ch != '。':
        raise ValueError(
            f"句读结果文件含非句号旧标点「{ch}」(U+{ord(ch):04X})，"
            f"疑似以原文导出文本冒充句读结果，已拒绝。")
```

**修复 C（验证层，预警重合度）** — `process()` 中在 `validate_and_align` 后：
- 统计"结果句号数量"与"IDML 原旧句号数量"，若两者相差 <5% 且结果文件非 `*句读结果*` 命名 → 打印警告"疑似以原文当结果"。

**验证**：`0080.txt` 冒充结果 → 必须报错；`0080句读结果.txt` → 正常通过。

### FIX-2【P1】batch 归档：rename_v2 后归档丢失 + 源文件冲突未收集 + 异常中断

**根因**：`archive()` 按固定名 `{name}导出_WD注入.idml` 查找输出（重命名后找不到）；`collect_conflicts()` 未收集 pending 源文件目标；`archive()` 无 try/except。

**修复** — `code/batch_inject.py`：
```python
# 1) main(): 把实际写出的 out_path 传给 archive
archive(name, decisions, actual_out_path=out_path)

# 2) archive(): 用 actual_out_path 定位输出，不再拼固定名
def archive(name, decisions, actual_out_path):
    out_src = actual_out_path          # 实际输出（可能是 _v2）
    ...
    # 3) 每个归档步骤包 try/except，单步失败记入警告不中断
    for step in (inj_copy, done_move, pending_move):
        try:
            step()
        except Exception as e:
            print(f"  归档步骤失败（已跳过）: {e}")

# 4) collect_conflicts(): 补收 pending 源文件目标
for f in os.listdir(PENDING_DIR):
    if _extract_number(f) == name:
        conflicts[os.path.join(DONE_DIR, name, f)] = f"done/{name}/ 源文件归档"

# 5) _smart_move 目标存在时优先 rename，避免 FileExistsError：
#    action='overwrite' 时先 shutil.copy2(src, dst) 再 os.remove(src)（同盘安全）
```

**验证**：批量跑两轮（第二轮选 R）→ 归档完整、无崩溃、本轮输出进入 injected/done。

### FIX-3【P1】idml_utils.py 旧版死代码

**修复**：三选一（推荐 ①）：
1. **删除** `code/idml_utils.py`（1695 行），inject.py 已是唯一实现；
2. 或改为薄兼容层：`from inject import *` + `__all__` 重导出；
3. 更新 progress.md/findings.md 中"可复用模块"表述。

**验证**：`grep -rn "idml_utils" code/ *.md` 无残留引用；回归通过。

### FIX-4【P1】CSR 拆分复制 Self（依赖数据源无 Self 属性）

**根因**：`_rebuild_paragraph_xml()` 拆分分支的 `clean_prefix` 只剥 Br/Group，未剥 `Self`；当前样本 CSR 无 Self 未触发，换 InDesign 版本即失败。

**修复** — `code/inject.py` `_rebuild_paragraph_xml()` 拆分分支：
```python
# clean_prefix 追加 Self 剥离（仅非首段使用）
clean_prefix = re.sub(r'Self="[^"]*"', '', clean_prefix)
```
同时 `_verify_structure` 检查 #1 的报错信息改为提示"拆分复制了 Self"。

**验证**：合成用例（带 Self CSR + 槽内句号）→ 输出无重复 Self。

### FIX-5【P1】多 Content 空槽 replace 错位

**根因**：`parts_xml.replace(old_ctag, new_ctag, 1)` 对相同 Content 文本（如多个 `<Content></Content>`）顺序替换会错位。

**修复** — `_rebuild_paragraph_xml()` 多 Content 分支：按索引定位替换：
```python
cursor = 0
for oi, ol in enumerate(orig_lens):
    old_ctag = f'<Content>{orig_contents[oi]}</Content>'
    pos = parts_xml.find(old_ctag, cursor)
    if pos < 0:
        raise ValueError(f"重建失败：Content 槽位 {oi} 未找到")
    parts_xml = parts_xml[:pos] + new_ctag + parts_xml[pos+len(old_ctag):]
    cursor = pos + len(new_ctag)
```

**验证**：构造含空槽多 Content CSR 合成用例 → 输出槽位正确。

### FIX-6【P1】正文 Story 内嵌 ACE 指令（`<?ACE N?>`）丢失风险

**根因**：is_special 记录不进入对齐与重建，所在 CSR 被 `_clear_content_*` 清空；`_verify_extraction`/`_verify_output` 双双排除 is_special → 验证不设防。当前 5 本经书 ACE 均在装饰 Story 未触发。

**修复** — `code/inject.py`：
1. `_rebuild_paragraph_xml()` 对**无记录 CSR 的 is_special Content**：若原 CSR 含 `<?ACE`，用 `_clear_content_keep_br` 保留空壳但**将 Content 恢复为原 ACE 文本**（而非清空）：
```python
# 无记录 CSR 分支中，替换为保留 ACE 的版本：
if orig_had_ace := '< ?ACE' in ' '.join(cdata.get('contents', [])):  # 语法示意
    repl = re.sub(r'<Content>.*?</Content>',
                  lambda m: m.group(0) if 'ACE' in m.group(0) else '<Content></Content>',
                  orig_csr)
```
2. `_verify_output` 增加 is_special 核对：输出提取的 is_special 集合与输入一致。

**验证**：构造正文内嵌 ACE 合成用例 → 输出保留 ACE；5 本经书回归不回归。

### FIX-7【P1】`--output` 可覆盖源 IDML

**修复** — `code/inject.py` `main()`：
```python
if os.path.abspath(args.output) == os.path.abspath(args.idml):
    print("错误: --output 不能与 --idml 同路径（会覆盖源文件）")
    sys.exit(1)
```

---

## 第二批：P2（一周内，预计 4-6 小时）

| # | 位置 | 修复 |
|---|------|------|
| P2-1 | `code/inject.bat` | 加 `-X utf8`（与 batch_inject.bat 对齐）；或 `inject.py` 入口 `try: sys.stdout.reconfigure(encoding='utf-8') except: pass` |
| P2-2 | `batch_inject.py find_pairs` | 同经号多文件：排序后取"文件名含句读结果/导出"优先级最高的，或要求唯一并报错 |
| P2-3 | `_should_suppress_punct` | 规则 A 的 `_get_prev/_get_next_non_ws_font` 增加 story/para 边界检查（font_prev 越界时按 None 处理）——**不改变抑制策略，仅堵跨 Story 取字体缺陷** |
| P2-4 | `test_verify_inject.py` | ① 按 designmap 动态定位主 Story，去硬编码 `Story_u15de.xml`；② 清理改用 `shutil.rmtree` 容错或记录残留 |
| P2-5 | `_verify_structure` 检查 #3 | 只检查 `split_sources` 指定的分割段落，而非全部段落（消除误报） |
| P2-6 | `batch_inject.py archive` | move 前对目标做 rename 决策（FIX-2 已含），杜绝覆盖旧归档 |
| P2-7 | `extract_from_result` | `---` 头部识别改为「首行 `#` 标题 + 空行 + `---`」三段式匹配；正文 `---` 不再截断（或明确拒绝正文含 `---` 的文件并报错） |
| P2-8 | `validate_and_align` | `_MIN_CLEAN_CHARS_PER_STORY` 提升为命令行参数（默认 50），文档说明装饰 Story 判定 |
| P2-9 | 文档 | progress.md "六层验证"→三层；findings.md 行数/内存决策；README 命令与 bat 对齐；目录结构图补新文件 |
| P2-10 | `process()` 内存 | `generate_idml` 完成后 `del stories` + `gc.collect()`（验证用 preloaded 不依赖 stories）；大经书（497）处理前先跑 `bench_inject.py` |
| P2-11 | 0080 增补字符 | 在 0080 输出样本上做 InDesign 打开验证（或在报告中标注"待实证"）；必要时重建时保留实体形式 |
| P2-12 | 461 双版本 | 与用户确认交付版本；`test_verify_inject.py` 与归档统一引用同一版本 |

---

## 第三批：P3（长期，随重构处理）

| # | 位置 | 修复 |
|---|------|------|
| P3-1 | CLAUDE.md / AGENTS.md | 二选一为唯一权威，另一份改为软链接/引用 |
| P3-2 | AGENTS.md | 规则编号补 30 或改连续编号 |
| P3-3 | inject.py | 删除死函数 `_remove_empty_punct_csrs`/`_insert_punct_csrs`/`_stream_write_idml`（约 100 行） |
| P3-4 | `_verify_br_count` | `br_cleared_text` 为负时打印提示而非负数 |
| P3-5 | `resolve_conflicts` | `input()` 包 EOFError 处理（无 TTY 时默认 skip） |
| P3-6 | `batch_inject.process_one` | 异常时打印 traceback（`traceback.print_exc()`） |
| P3-7 | 工作区 | 清理根目录 `0080.txt/175导出.txt/275导出.txt/461导出.txt`（移入 source/ 或归档）；清理 output/ 残留 `_` 前缀测试产物 |
| P3-8 | 进度条 | `print(..., end='')` 在非 TTY 时退化为普通日志（检测 `sys.stdout.isatty()`） |
| P3-9 | issues-checklist | 遗留项逐一关闭或迁移（类型注解、logging、requirements.txt、CI、模块拆分 #13） |
| P3-10 | inject.bat 中文注释 | 改英文或加 BOM |
| P3-11 | `html.unescape` | 对非法实体（U+FFFD 替换结果）计数并警告 |

---

## 回归测试计划（每批修复后必跑）

```bash
# 1) 核心回归（5 本经书）
python -X utf8 code/test_verify_inject.py        # 275 + 461
python -X utf8 code/test_zip_stream.py            # ZIP 流式 14 项

# 2) 附加经书全流程（35/0080/3093）
python -X utf8 -c "import sys; sys.path.insert(0,'code'); import inject;
inject.process('source/35导出.idml','source/35_WD句读结果.txt','output/_t35.idml')"
python -X utf8 -c "…0080…" 
python -X utf8 -c "…3093…"

# 3) P0 专项：错误配对必须失败
python -X utf8 -c "…inject.process('source/0080.idml','0080.txt',…)"  # 预期 ValueError

# 4) 批量演练：pending 放 275 副本 → 跑两轮（第二轮选 R）→ 归档完整
# 5) 合成用例（FIX-4/FIX-5/FIX-6 的单元断言）
```

**回归基准**（修复后必须与下表一致或更好）：
| 经书 | 字数一致 | 句号 | Br |
|------|---------|------|-----|
| 275 | 5684 ✓ | 1064→1054 | 41→39 |
| 461 | 9208 ✓ | 1517→1517 | 45→43 |
| 0080 | 2929 ✓ | 249→247 | 72→72 |
| 35 | 10035 ✓ | 1517→1002 | 161→161 |
| 3093 | 5284 ✓ | 921→921 | 68→68 |

---

## 风险与回退

1. **FIX-1B（拒绝非句号旧标点）**：若历史句读结果文件含残留逗号/顿号 → 会报错。处理：报错信息列明首字符与位置，人工清理后重跑；不改动工具时可用 `--skip-punct-check` 开关绕过。
2. **FIX-6（ACE 保留）**：若 InDesign 对正文 ACE 有特殊语义（如字形替换），保留可能影响渲染——先合成验证再决定。
3. **任何 P1 修复**：修改前 `git stash` 或独立分支（`fix/p0-p1`），验证通过后合并回 dev。
4. **497 大经书**：不在本方案范围内处理，但 P2-10 内存优化后应补一次 497 压测再交付。

---

*方案与审计报告配套使用。执行顺序：0.1→0.2→FIX-1→FIX-2→FIX-3→回归→FIX-4/5/6/7→回归→第二批→第三批。*
