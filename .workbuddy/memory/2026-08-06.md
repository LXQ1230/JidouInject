# 2026-08-06 工作日志

## P3-14 进度条与性能修复（v1.4.2 → v1.4.3）

### 问题 1：解析 IDML 进度条走不到 100%
- 根因：所有 IDML 的 designmap StoryList 声明数 > 实际 Story 文件数（缺 ub3/uac，扫描 15 个文件全部存在），旧代码分母用声明数、循环 continue 跳过缺失 → 进度卡 97.x%
- 修复：`extract_from_idml` 分母改用实际可用 Story 数，namelist 缓存为 set，打印缺失清单
- 验证：497 解析进度 38/38 = 100%（旧 38/39 = 97.4%）

### 问题 2：497 生成输出卡 97.1% 十分钟不动
- 根因链：497 的 Story_u562 解压 71MB / 581 段 / 25.8 万 CSR；写回循环 i=340 显示 97.1%（340/350）后，i=347 处理 u562 → `_rebuild_paragraph_xml` 第 5 步从后往前逐 CSR 替换 O(K×L) 二次方退化 + 4319 个分割段每段全模板扫描 → 单 Story 重建 >15 分钟未完成（实测）。用户中断后残留 output/497导出_WD注入.idml.tmp（986KB，前 346 成员已写，卡在 u562 重建前）
- 修复：
  1. `_rebuild_paragraph_xml` 第 5 步改一次拼接 O(L)（u562 重建 15min+ → 59.4s）
  2. 分割段空壳 CSR 用 `_DROP_CSR` 哨兵删除（输出解压 1364MB → 219MB）；注意 PSR 开标签必须无条件保留（曾导致 275 段落数 9→8 回归失败，已修）
  3. `generate_idml` 大 Story 重建前显示状态消息 + 循环结束补 100%
  4. `_verify_structure` 第 3 部分按 Story 缓存 PSR 列表（O(n²) → O(n)，验证 18min+ → ~10s）

### 结果
- 497 完整流程（新句读结果 pending/497有图_WD句读结果.txt）：旧代码卡死 >15min → 新代码 **78.6s** 完成，624,801 字符验证通过，输出 3.9MB 压缩
- 回归 reg_p314.py：461/3093 字节一致，275 内容一致（空壳瘦身 7KB），全部通过

### 备份
- backup/inject.py.v1.4.2-progress-perf-2026.08.06.bak
- 新增回归脚本 code/reg_p314.py（内容级对比回退逻辑）

## v1.5.0 标点开放（v1.4.3 → v1.5.0）

### 需求（用户已确认三项决策）
- 回注标点白名单：仅「。」 → `_INJECTABLE_PUNCT = '，、；：？！。'`（7 种常用中文标点）
- 抑制规则：对新标点一视同仁（仿宋/楷体区域抑制、思源宋体 U+3000 间隔抑制）
- 样式：沿用 CharacterStyle/句号 模板，强制思源宋体

### 改动点（code/inject.py）
1. 新增 `_INJECTABLE_PUNCT` frozenset（_OLD_PUNCT_CHARS 的子集）
2. FIX-1B：`ch != '。'` → `ch not in _INJECTABLE_PUNCT`（白名单外旧标点仍拒绝）
3. `validate_and_align`：`ch == '。'` → `ch in _INJECTABLE_PUNCT`；CharRecord char 用实际标点；compare 排除集合改为白名单
4. **关键修复**：`_rebuild_paragraph_xml` 标点 Content 原取自模板（硬编码「。」），新标点会被写成句号 → 新增 `_punct_content_tag(char)`（拆分分支）与 `_punct_csr_from_template(template, char)`（旧句号 CSR 复用分支），Content 替换为实际标点；纯句号时字节与旧实现完全一致
5. FIX-1C 重合度预警统计口径扩展到全部白名单标点；Step2 打印标点分项统计

### 验证
- reg_p150.py（新代码 vs backup/inject.py.v1.4.3-punct-expand-2026.08.06.bak）：461/275/3093 输出 ZIP 全成员字节一致 ✓
- reg_p150_mixed.py：461 结果程序化混合 7 种标点（1545 个）全部注入、输出统计与结果逐项一致；「」等白名单外标点仍被 FIX-1B 拒绝；1248 个新标点 CSR 全为思源宋体 ✓
- reg_p150_497.py：497 新句读结果重跑（81.5s，624,801 字符验证通过），与 v1.4.3 基线输出 ZIP 全成员字节完全一致（350 个成员）✓
- 备份：backup/inject.py.v1.4.3-punct-expand-2026.08.06.bak

## git 提交（14:30）
- 用户指示"记录目前的进度和状态，同步更新并提交"：更新 progress.md（补 P3-14 + v1.5.0 阶段记录），随后拆分两个提交推送：
  1. `8fd05ab fix(batch): find_pairs 空列表崩溃修复 + 配对放宽`（batch_inject.py + memory 08-04）
  2. `bf974ce feat(v1.5.0): 标点白名单开放 + P3-14 性能修复 + 回归脚本与文档同步`（inject.py + reg/diag 脚本 + progress/fix-plan + memory 08-06/MEMORY.md + source 497 新句读结果等）
- 状态：origin/dev 从 33fa16a 推进至 bf974ce（含此前 6 个本地领先提交），本地与远端完全同步；工作区干净；pending/ 已空（497 新句读结果已重跑完毕）

## 免 Python 环境打包（v1.5.0 GUI + 拖拽双模式 exe）

### 需求（用户确认）
- 打包工具：**Nuitka**；功能：**单文件+批量合一**；交互：**拖拽/命令行 + GUI 都要**

### 产物与交付
- 单文件 exe：`dist/IDML句读回注工具.exe`（Nuitka 4.1.3, --onefile, --windows-console-mode=disable, --enable-plugin=tk-inter, --mingw64）
- 使用说明：docs/打包版使用说明.md；重打包脚本：code/build_exe.bat

### 架构设计（重要，防复发）
- **code/launcher.py 统一入口**：拖 ≥2 个文件（isfile 判定）→ CLI 拖拽模式（构造 sys.argv 复用 inject.main() 全部校验）；无参数 → tkinter GUI（gui_inject.run()）；1 个文件 → 提示退出
- **windowed exe + AllocConsole 双模式**：GUI 子系统无黑窗；拖拽时 `_alloc_console()` 动态建控制台 + 重定向 CONOUT$/CONIN$（UTF-8）。测试环境变量 `JIDOU_NO_CONSOLE=1` 跳过（沙箱 GetConsoleWindow()==0 会误 AllocConsole 弹窗吞输出）
- **code/gui_inject.py**：tkinter 纯标准库；后台线程跑 inject.process()，QueueWriter 重定向 stdout/stderr 到 queue，主线程 root.after(100) 轮询刷新 ScrolledText；QueueWriter.isatty()=False 使 _log_progress 退化为换行日志（\r 进度条 GUI 不可用）；单文件冲突弹窗 askyesnocancel（覆盖/改名/取消）；批量模式用户自选目录 + 参数化 find_pairs_in_dir（batch_inject 的 find_pairs 写死项目路径不可复用）+ 输出到所选目录 output/ 子目录 + 冲突自动 rename_v2 绝不覆盖
- **input() 健壮性**：拖拽模式结尾 input() 等待回车（窗口停留），异常捕获 (EOFError, OSError)——沙箱 bash 的 /dev/null 重定向会让 Windows python 的 input 抛异常，不可只捕 EOFError

### 关键经验（测试环境坑）
- Git Bash 的 `/tmp/...` 虚拟路径 Windows 原生 python 不认 → 测试用 Windows 路径临时目录
- 沙箱 `subprocess.run` 不指定 stdin 时子进程继承管道会永久阻塞 input() → 测试必须 stdin=subprocess.DEVNULL
- 沙箱 rmtree 删除失败（recycle-bin 不可用）→ 测试临时目录会残留
- 打包 venv：`pack312`（系统 Python 3.12.10 有 tkinter；managed 3.13.12 无 tkinter）
- Nuitka 打包耗时 15-30 分钟（含首次 MinGW 自动下载）

### 测试
- code/test_gui_logic.py（纯逻辑：配对/校验/QueueWriter）全通过
- code/test_cli_drag.py（subprocess 模拟拖拽 + 与 python 直跑 inject.py 输出**字节级一致** 341565B）通过
- GUI 冒烟测试（Tk 构建 + 事件循环）通过

### 最终交付状态（16:25）
- **产物：`dist/IDML句读回注工具.exe`（6.5 MB，Nuitka onefile，windowed）**，三重验证全通过：
  1. 拖拽模式：exit 0 + 与 python 直跑 inject.py **字节级一致**（341565B）
  2. GUI 模式：沙箱中 15s 稳定运行（mainloop）；此前 bash 工具启动快速退出是沙箱环境偶发交互问题，subprocess 方式启动正常
  3. 临时目录：`--onefile-tempdir-spec='{TEMP}/jd_onefile_{PID}_{TIME_US}'` 生效，正常退出自动清理，工作目录零残留
- **Nuitka 关键参数与坑（防复发）**：
  - `--windows-console-mode=disable`（GUI 子系统无黑窗）+ launcher AllocConsole 拖拽弹窗
  - `--mingw64 --assume-yes-for-downloads`（无 MSVC 时自动下载；非交互环境必须加 assume-yes 否则 Dependency Walker 下载被拒 FATAL）
  - **tempdir-spec 语法是 `{TEMP}` 花括号**（%TEMP% 百分号语法不识别、静默回退 CWD）
  - MinGW64 156MB 下载是首次打包耗时大头（国内 ~14 分钟）；ccache 缓存后重打包 48-90 秒
  - exe 文件被占用（残留 GUI 进程）会导致打包 PermissionError → 重打前先杀进程删旧 exe
- **遗留（沙箱删除受限）**：`jd_exe_test.hw1qkh/` 测试目录 + `code/nuitka-crash-report.xml` 需手动清理
- 使用说明：docs/打包版使用说明.md；重打包脚本：code/build_exe.bat（含全部正确参数）
- 测试环境变量 `JIDOU_NO_CONSOLE=1`（跳过 AllocConsole，沙箱 GetConsoleWindow()==0 会误弹窗吞输出）

## v1.5.1 Br 分行保留修复（v1.5.0 → v1.5.1）

### 触发
- 批量处理 pending/26-A.idml（發覺淨心經，偈颂体）失败：`Br 丢失过多: 输入 136 → 输出 107（最低预期 124）`

### 根因（偈颂 Br 首次暴露，全部在 Story_u15de）
- 26 号所有 Br 在 Story_u15de（其余 25 Story 为 0）；**偈颂分行 = 多 Content CSR 内嵌 `<Br />`**（`<Content>一行</Content><Br /><Content>二行</Content>`），Br 也出现在 CSR 前缀（行首）
- **Bug A（丢 18）**：无记录旧句号 CSR（Content='。' 但样式为 `[No character style]`）走 A1 else 被**整体删除**（含 Br）。26 号 PSR[0] 有 18 个此类 CSR——偈颂分行符全丢
- **Bug E（分割副本丢 Br）**：多 Content 分割副本 `if is_split_copy: parts_xml = re.sub('<Br','')` 剥掉全部 Br（自 v1.4.1 起的遗留）→ 26 号分割副本 31 Br → 2 Br
- **验证口径**：`br_in_old_punct` 的 csr_pattern 只认 `CharacterStyle/句号` 样式，漏掉 Content='。' 的（is_punct 判定不一致）→ 误报

### 修复（code/inject.py，4 处）
1. **A1 分支**：无记录 is_punct CSR 带 Br 且非 leading → `_clear_content_keep_br` 保留（清空 Content + Br）；leading 仍删除（防段首空行——275 回归「分割段落 Para[7] leading Br」）
2. **多 Content 分割副本**：删除 is_split_copy 剥 Br 逻辑（A3 空壳删除已覆盖 leading，有记录 CSR 的 Br 必须保留）
3. **验证口径**：`_verify_structure`/`_verify_br_count` 的 is_punct 判定与重建一致（样式 OR Content=='。'）
4. **分割豁免**：br_min 扣除分割源段 trailing 区域（最后一个文字记录 CSR 之后）的无记录 CSR Br——分割移走文字后该区域 Br 剥离属预期清理

### 重要教训（防复发）
- **Bug B 误判**：最初以为「拆分时 csr_suffix 从 rfind('</CharacterStyleRange>') 截取丢 suffix Br」——**错误**。v1.4.3 的 csr_suffix 本来就是 `csr_xml[content_end:]`（含 Br），parts[-1] 用它保留。误加修复导致 3093 拆分中间段/末段多出格式化空白（`\n\t\t\t`），**已撤销**。教训：修改前必须确认变量定义位置（1507 行 csr_suffix 与 1591 行 clean_suffix 不同）
- 461/275/497 与 v1.4.3 基线出现**字节差异是修复 1/2 的正确效果**（保留原文 Br 分行）：461 +1 Br、275 +2 Br、497 +296 Br（1641→1619，基线旧代码丢 318 个）；字符内容全部零差异
- reg_p150_mixed 测试 3 统计 bug：`c in PUNCTS` 对空串误判 True（空串是任何串子串）→ 修复 1 保留的空+Br CSR 被当非思源宋体标点 → 测试脚本加 `c and` 前置判断

### 回归（全部通过）
- reg_p150.py：3093 字节一致；461/275 内容一致（Br 分行保留，+1/+2）
- reg_p150_mixed.py：混合标点 7 种全注入 + FIX-1B 负例 + 1248 标点全思源宋体
- reg_p313.py / reg_p314.py：全部通过（已加内容级回退）
- reg_p150_497.py：497 重跑 89.3s，624,801 字符验证通过，字符内容与 v1.4.3 基线零差异
- 26-A：**验证通过**（13963 字符一致），Br 136→141（旧句号分行保留 + 分割副本新增）
- 备份：backup/inject.py.v1.5.0-br-loss-2026.08.06.bak

