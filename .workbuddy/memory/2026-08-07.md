# 2026-08-07 工作日志

## 35 号文件混合标点问题诊断（讨论方案，未实施）

### 用户两个需求
1. **括号保留**：IDML 原文有「（）」等括号，txt 对应位置没有 → 希望回注时保留 IDML 原位置的括号
2. **顿号/逗号导致换行**：35 用混合标点 txt（815句号+702逗号+190顿号）回注后，IDML 中「時、離垢施」换行/换段；只有句号 txt（35_WD句读结果.txt）该位置「時離垢施女則」（无标点）正常

### 问题 2 根因（已确认）
- 原始 IDML 中多字 CSR（如「離垢施女」「則為梵志而說頌曰」）是单 Content 单 CSR
- `_rebuild_paragraph_xml` 中，标点分配到 `after_csr`=前邻文字 CSR → `has_punct_inside=True` → 走拆分路径 → **每个文字 segment 生成独立 CSR**（逐字拆分）
- 实测：35 输出 Story_u621.xml 1.05MB→2.87MB（2.73 倍）；CSR 3651→9396；单字 Content 1922→8640
- 只有句号版同样拆分（8632 CSRs）但该位置无标点，视觉正常；混合版顿号/逗号在 CSR 边界触发拆分 + InDesign 标点挤压（顿号禁行首）→ 换行
- 输出内容无丢失（净文本 11227 chars = 原文 11040 + 注入标点），验证能通过，但排版被破坏

### 修复方案（讨论中）
- **方案 A（推荐）**：拆分路径合并连续文字 segment——文字保持原 CSR 完整文本，仅标点生成独立 punct CSR（「等輩求諸利義必不如意」+「。」，而非逐字拆分）
- 方案 B：punct 位于 CSR 末尾时分配到下一个 CSR 前（改动对齐逻辑，面大）
- 回归：461/275/3093 字节级会失败（旧拆分 vs 新合并），回退内容级对比

### 问题 1 方案（讨论中）
- 把括号字符在解析时标记保留（类似 U+3000/ACE 处理），对齐时跳过（不消费 txt），重建时保留原 Content
- 需用户确认保留范围：全角（）？半角()？书名号/引号？

### 35 数据
- 35_WD句读结果.txt（只有句号 1517）vs 35_WD句读结果_vs_35_对比文本_20260807-0900.txt（混合 815+702+190）
- 输出 35导出_WD注入.idml 已生成（混合版）；/tmp/35_only_period_test.idml 为只有句号版测试输出

## 方案确认（10:13，用户已确认，先落文档不实施）

### 方案 C（问题 2 修复）最终设计
- **核心**：末尾标点重分配——文字 CSR 的末尾标点（after_pos=CSR 末字符）转移给紧随的旧句号 CSR（is_punct 且无 segments），激活 v1.5.0 死代码「旧句号 CSR 复用分支」
- **三个改动点**（全在 `_rebuild_paragraph_xml`）：
  1. 重分配预处理（csr_segments 构建后，约 20 行）
  2. 复用分支改造：用 CSR 自身模板（新增 `_punct_csr_from_own_template`，优先替换含「。」的 Content），副本剥离 Group/Self，多标点去 Br 拼接
  3. 拆分路径合并连续文字段（兜底）：无法重分配的标点不再逐字拆 CSR
- **用户确认**：标点红色样式问题忽略，不做额外处理
- 预期：35 号 CSR 9396→约3651、体积 2.87MB→约1.05MB、无换行

### 分层验证（回归体系，用户已确认）
- 基线 = v1.5.1（当前 code/inject.py，备份目录无 v1.5.1 备份，需新建 baseline）
- L1 净文本对比（铁律，story_text_contents 复用，差异输出文字/标点双序列 diff）
- L2 结构指标（br/psr 必须相等硬性；csr 只减不增；single1 降≥50%；oldpunct 与原始一致±10%；emptycsr 显著下降；preserved_multichar≥95%）
- L3 样式抽查（文字 CSR 抽样三元组 vs 原始；复用句号 CSR Content∈白名单+FillColor 保留+Br 保留）
- 验证矩阵：461/275/3093（基线对比）+ 35（vs txt 专项）
- 新回归脚本 code/reg_v152.py

### 问题 1 括号保留（方案确认，实施排后）
- 成对符号白名单常量 `_KEEP_BRACKETS`，默认：（）()〔〕《》〈〉「」『』【】［］
- 项目实测：全角圆括号 54（20 号藏本校勘注）、龟甲括号 16（3093「慈〔云〕懴主」）；964 个"半角尖括号"是 ACE 指令误报
- 实现：解析标记 is_bracket → 对齐跳过（不消费 txt）→ 重建原样保留；txt 含括号仍拒绝
- 验证调整：L1 净文本允许括号增量豁免；输出自检排除括号；story_metrics 加 bracket 指标

### 方案文档
- 完整方案已写入 `plan-csr-reuse-and-brackets-2026-08-07.md`（项目根目录），用户确认后实施

## v1.5.2 方案 C 实施完成（10:15-11:40，回归全部通过 ✓）

### 实施内容（code/inject.py 三个改动点，已备份 v1.5.1 baseline）
- 备份：`backup/inject.py.v1.5.1-csr-reuse-baseline-20260807.bak`
- 改动点 1：步骤 3.7 末尾标点重分配（标点在 CSR 末槽位且 after_pos ≥ len(orig_contents[slot])-1，后邻 segments 为空的旧句号 CSR → 转移）
- 改动点 2：`_punct_csr_from_own_template` + 复用分支用自身模板（副本剥离 Group+Self、多标点去 Br 补 Br）
- 改动点 3：拆分路径 blocks 合并连续文字（兜底）
- **实施中发现并修复的 bug**：拆分路径标点块继承前邻文字块 csr_prefix 时带上行首 Br（461 回归 Br 44→45）→ 标点块前缀剥离 Br

### 回归结果（code/reg_v152.py，三层验证全部通过 ✓）
| 用例 | L1 净文本 | csr | single1（原始） | preserved | 体积 |
|---|---|---|---|---|---|
| 461 | 新旧一致 ✓ | 10323→3541 | 1898（原始 1629）✓ | 86.4%（提示，数据特性） | - |
| 275 | 新旧一致 ✓ | 5885→3958 | 2674（原始 2669）✓ | 99.5% ✓ | - |
| 3093 | 新旧一致 ✓ | 5720→2018 | 1027（原始 1025）✓ | 99.9% ✓ | - |
| 35 混合 | 正文文字 vs txt 一致 ✓ | 3681→4047（≤1.5×） | - | 190「、」+815「。」复用 ✓ | 283KB→284KB（v1.5.1 是 2.87MB）|

- 461 preserved 86.4% 为数据特性（txt 句读密度高 + IDML 长句块多，句号在 CSR 中间属必要拆分），非 bug
- 35 号「時、離垢施女、則為梵志而說頌曰」无逐字拆分（「離垢施女」整块保留）
- 遗留：35 号重注产物建议 InDesign 人工确认（计划 7.5）；497 新句读结果可后续重跑（性能只升不降）
- 问题 1 括号保留：按计划 5.4/6.3 节为后续步骤，待另行确认后实施

## v1.5.3 问题 1 括号保留实施完成（10:52-11:05，回归全部通过 ✓）

### 实施内容（code/inject.py，已备份 v1.5.2 baseline）
- 备份：`backup/inject.py.v1.5.2-brackets-baseline-20260807.bak`
- `_KEEP_BRACKETS` 白名单 + `_is_keep_bracket`；CharRecord 加 `is_bracket` 字段（默认值区）
- 解析：括号 `is_punct=False + is_bracket=True`（不再是旧标点）
- `_is_ws_for_compare` 对括号返回 True（对齐不消费 txt、字体查找跳过、比对排除）
- 对齐 while ws 分支：括号作为文字记录加入（重建 Content 原样保留）
- 统计口径同步：story_clean_count 与 _verify_output 的正文判定排除 is_bracket（防装饰 Story 误判）

### 回归结果（reg_v152.py，基线切换为 v1.5.2，全部通过 ✓）
- 461/275：无括号文件，新旧输出**完全一致**（csr/single1/emptycsr 零变化）→ 括号改动零回归
- 3093：**bracket 2→2 零丢失**（「慈〔云〕懴主」完好），csr 2018→2018
- 35：混合标点不回归（正文 10035 vs txt 一致、体积 284KB）

### 20 号阻塞（独立问题，留待后续）
- `pending/20.idml` 正文含 ■□◎◇▲△ 三角方格装饰符号（20带三角方格等符号.txt），txt 无 → 对齐验证失败（净文字 14828 vs 14822）
- 三角符号非括号，超出问题 1 范围；20 号 54 个全角圆括号保留待此问题解决后一并验证

## 20 号三角符号方案确认与实施（10:57-11:15，用户确认：等同空白保留 + 几何符号全集）

### 数据调查结论
- 20 号符号构成：几何装饰符号 ■□◎◇▲△○ 共 7 个（正文「如■是□我◎聞一◇時佛▲住△…」字间镶嵌 + 「卷第十七○原本為」）+ 校勘注全角圆括号 9 对（（甯）（磧）（清）（大）（卍）（麗）（宋）（元）（明））+ ．40/.1 旧标点（自动清除）
- **题记不是障碍**：「唐三藏法師菩提流志奉詔譯」等 60 字在装饰 Story（12 字 <50）自动跳过
- 排除符号后 IDML 文字 == txt（14821 字，逐字 0 差异）——几何符号是唯一阻塞

### 实施（v1.5.3 补全）
- `_KEEP_ORNAMENTS` 常见几何符号全集（约 30 个：■□△▲○●◇◆ + 方块/三角/圆变体 + ★☆✦✧※）
- 统一入口 `_is_keep_symbol`（= _KEEP_BRACKETS + _KEEP_ORNAMENTS），解析/`_is_ws_for_compare` 改用
- 备份：`backup/inject.py.v1.5.3-brackets-baseline-20260807.bak`

### 回归结果（全部通过 ✓）
- 461/275/35：零回归（无符号或符号在装饰 Story，csr 不变）
- 3093：保留符号 7→7（龟甲括号 2 + 几何 5）
- **20 号**：L1 正文文字 vs txt 一致（14821 字）、csr 5057→7472（≤1.5×）、体积 297KB→307KB、**保留符号 25→25 零丢失**、句号复用 2386/2425（98.4%）
- 20 号产物可交付：pending/20.idml + pending/20_WD句读结果.txt → injected/（待用户确认后正式输出）
